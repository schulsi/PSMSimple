extends: default

rules:
  line-length:
    max: 130
    level: warning

  truthy:
    allowed-values: ["true", "false", "on", "off"]

  comments:
    min-spaces-from-content: 1

  document-start: disable

ignore: |
  node_modules/
  .venv/
